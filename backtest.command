#!/bin/bash
# Lotto AI Backtest Runner

cd "$(dirname "$0")"

echo "================================================"
echo "🔬 로또 AI 알고리즘 신뢰도 검증 (백테스팅)"
echo "================================================"

if [ -d "venv" ]; then
    source venv/bin/activate
fi

export PYTHONPATH=$PYTHONPATH:.
# 최근 100회차를 대상으로 시뮬레이션 실행
python3 main.py --backtest --last 100

echo ""
echo "================================================"
echo "검증이 완료되었습니다. 창을 닫으려면 아무 키나 누르세요."
read -n 1
